# HTTP-Port für Zugriff im LAN
HTTP_PORT=8080

# HTTPS-Port (optional, falls TLS genutzt wird)
HTTPS_PORT=8443

# Optionaler Debug-Port direkt zur App (nur bei Bedarf in compose ergänzen)
DEBUG_WEB_PORT=

# Optional build metadata (CI can set these)
APP_BUILD=dev
GIT_SHA=local
BUILD_DATE=
